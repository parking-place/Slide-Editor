# Unreleased

다음 릴리즈에 포함될 변경 사항을 여기에 기록합니다.

## Added

- 프로젝트 저장, 불러오기, 다른 이름으로 저장을 브라우저 `prompt` 대신 전용 모달 UI에서 처리하는 프로젝트 관리 패널을 추가했습니다.
- 프로젝트 이름 변경과 삭제 기능을 프로젝트 매니저 모달에 추가했습니다.
- 슬라이드 생성/수정의 이미지 업로드 영역에 드래그앤드롭 업로드를 추가했습니다.

## Changed

- 헤더의 `New`, `Open`, `Save As` 흐름을 테마 모달과 비슷한 고정형 UI 패턴으로 전환했습니다.
- 헤더의 프로젝트 액션 버튼들을 `Project Manager` 버튼 하나로 통합했습니다.

## Fixed

*(none)*
