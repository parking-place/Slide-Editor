# Unreleased

다음 릴리즈에 포함될 변경 사항을 기록합니다.

## Added

- 없음

## Changed

- 이미지 파일을 선택하면 업로드 상태가 즉시 `등록됨`으로 바뀌고, 슬라이드 생성 직후 이미지 박스에 바로 `변환 중` 상태가 표시되도록 UX를 정리했습니다.
- 여러 슬라이드에서 이미지 업로드가 연속으로 발생해도 요청이 꼬이지 않도록, 에디터에서 업로드를 백그라운드 큐로 직렬 처리하게 바꿨습니다.

## Fixed

- 에디터 미리보기와 Viewer HTML에서 본문이 길어질 경우 슬라이드가 기본 높이 이상으로 자연스럽게 확장되도록 수정했습니다.
- Viewer에서 중제목이 바뀔 때 에디터와 같은 구분 커버 슬라이드가 표시되도록 맞췄습니다.