# Unreleased

다음 릴리즈에 포함될 변경 사항을 여기에 기록합니다.

## Added

- 프로젝트 저장, 불러오기, 다른 이름으로 저장을 브라우저 `prompt` 대신 전용 모달 UI에서 처리하는 프로젝트 관리 패널을 추가했습니다.

## Changed

- 헤더의 `New`, `Open`, `Save As` 흐름을 테마 모달과 비슷한 고정형 UI 패턴으로 전환했습니다.

## Fixed

*(none)*
